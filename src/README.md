# Source

Main implementation code will live here.

The first CLI-first orchestration shell starts in `scripts/`, not `src/`, so the repo can
gain a real local entry surface without implying a broader runtime exists yet.
